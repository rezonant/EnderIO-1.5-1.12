if (project.include_in_build || !gradle.startParameter.taskNames.contains("build")) {
	dependencies {
	  	//implementation "refinedstorage:refinedstorage:${refinedstorage_maven_version}:api"
		implementation project(":enderio-base")
		implementation project(":enderio-conduits")
	}
}